<form method="post" action="{{ path('dossier_suspension_delete', {'id': dossier_suspension.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dossier_suspension.id) }}">
    <button class="btn btn-danger btn-lg">Delete</button>
</form>
